# V5.1 驗證紀錄

## 結果

以 Node.js node:test／VM 執行 30 項回歸測試，全部通過。執行方式：

```sh
node --test tests/regression.cjs
```

另以原始附件比對：6,012 筆詞條的全部 10 個欄位及 word ID 一致；原有 795 條句型與中文釋義全部保留。收藏、搜尋偏好、搭配練習、最近搜尋與複習排程 key 一致。AI 索引 key 因內容更新而改版，不涉及使用者進度。

636 筆來源的定位字串與短摘錄均通過指定年度／PDF 頁碼的文字比對；資料覆蓋 111–115 五年。這是出處驗證，不是聲稱 636 個片語都是官方測驗正解。

## 新增功能驗證

- 883 組／1,295 條統計、611 條已核對句型與 609 組有來源考點一致。
- 每筆來源正確對應句型位置、年份、PDF 頁碼、印刷頁碼、題組及來源類型。
- 所有有來源的考點都能從原詞表的相關詞條查到；新增英文片語與中文概念可以搜尋。
- 各年度分別篩選列表、詳情與練習；引用不跨入未選年度。
- 來源畫面區分本文、題幹與選項；未附逐句出處的延伸內容不假標成真題。
- 來源文字 HTML 跳脫，避免將摘錄當成可執行標記。
- 129 種去重練習的每題選項四個且不重複；回填正解可重建原句型，選項並列的介系詞不直接自動挖空。
- 切換篩選只重繪搭配區，不重新請求線上資料，也不改寫收藏。

## 保留的 V5 回歸驗證

語法與相對資源路徑、兩個入口一致、manifest icons、舊收藏讀取、未知收藏 ID 保留、儲存額滿處理、JSON 備份合併、損壞資料、整字／變化形比對、近似例句去重、拒絕劣質例句、中文／錯字／片語搜尋、完整啟動、請求去重及取消、併發上限、429 冷卻、快取持久化與過期、離線未命中、分區漸進更新、A→B→A 防串詞、API 全失敗保留內建資料、已有翻譯不重送。

## 體積

| 項目 | 原單檔 | V5.1 |
| --- | ---: | ---: |
| 入口 HTML | 2,120,077 bytes | 9,223 bytes |
| HTML＋直接引用 CSS／JS／標誌 | 2,120,077 bytes | 1,604,956 bytes |
| 同內容 gzip 估算 | 356,506 bytes | 434,702 bytes |

資料大幅增加，首次壓縮傳輸估算高於原版；入口變小不等於整站載入同幅加快。後續改善主要是 CSS／JS 各自快取、API 請求去重與持久快取、先顯示已完成區塊。數字不含按需 AI 模型、API 回應、HTTP 標頭與主機壓縮設定。

## 驗證界線

本環境沒有可用瀏覽器執行檔，未完成實機視覺驗證、iPhone／Safari 音訊播放、AI 模型下載或第三方 API 的實際延遲量測。DOM／IndexedDB 使用測試替身，API 使用模擬回應；這些測試驗證程式流程，不能當成正式站網速或所有瀏覽器相容性的保證。來源 PDF 文字擷取與頁碼核對使用實際附件。
